// Исправленный код:

// Ошибка №1 и №2: Исправлено сравнение с 'admin', логика для обычного пользователя
let user = 'admin';
if (user === 'admin') { // Используем строгое сравнение ===
  // Ошибка №3: Исправлен символ переноса строки
  console.log("Привет, админ!\nПроверь нет ли жалоб от пользователей!");
} else {
  console.log('Здравствуйте ' + user + '! У нас новые поступления халвы!');
}

let newOrders = 6;
let errorOrders = 3;
// показываем сколько новых заказов, а сколько заказов с ошибкой
if (newOrders > errorOrders) {
  // Ошибка №4: Исправлена опечатка в имени переменной errorOreders -> errorOrders
  console.log('В магазине ' + newOrders + ' новых заказов (с ошибкой: ' + errorOrders + ')');
}

// Эта проверка осталась, но теперь она работает корректно, если бы newOrders === errorOrders
// В текущем сценарии (6 > 3), она не выполнится.
if (newOrders === errorOrders) {
  console.log('ВНИМАНИЕ! Что-то идет не так! Все новые заказы завершились ошибкой!');
}
